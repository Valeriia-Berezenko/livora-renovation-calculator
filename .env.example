# Public Web3Forms access key (safe to expose in the browser).
# Create one at https://web3forms.com and set it in Vercel → Settings → Environment Variables.
VITE_WEB3FORMS_ACCESS_KEY=

# Auth is off for this app.
VITE_AUTH_ENABLED=false
